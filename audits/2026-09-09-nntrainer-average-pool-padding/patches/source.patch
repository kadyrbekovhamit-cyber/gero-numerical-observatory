diff --git a/nntrainer/layers/pooling2d_layer.cpp b/nntrainer/layers/pooling2d_layer.cpp
index 04e0da1..b4e5861 100644
--- a/nntrainer/layers/pooling2d_layer.cpp
+++ b/nntrainer/layers/pooling2d_layer.cpp
@@ -213,8 +213,11 @@ void Pooling2DLayer::calcDerivative(RunLayerContext &context) {
 
   auto apply_average = [&](auto *result_data) {
     using T = std::decay_t<decltype(*result_data)>;
-    int height_stride_end = height - p_height + pt;
-    int width_stride_end = width - p_width + pl;
+    // The final window is determined by bottom/right padding, as in forward.
+    int height_stride_end =
+      height - static_cast<int>(p_height) + static_cast<int>(padding[1]);
+    int width_stride_end =
+      width - static_cast<int>(p_width) + static_cast<int>(padding[3]);
     const int *iter = pool_helper.getData<int>();
     for (unsigned int b = 0; b < batch; ++b) {
       for (unsigned int i = 0; i < channel; ++i) {

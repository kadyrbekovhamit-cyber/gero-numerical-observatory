cmake_minimum_required(VERSION 3.25)
project(mlx_layernorm_bias_audit LANGUAGES C CXX)
set(CMAKE_CXX_STANDARD 20)
if(NOT MLX_SOURCE)
  message(FATAL_ERROR "Set MLX_SOURCE to a disposable pinned MLX source directory")
endif()
add_subdirectory("${MLX_SOURCE}" mlx-build)
add_executable(layernorm_regression native_regression.cpp)
target_link_libraries(layernorm_regression PRIVATE mlx)
